# sweasy

Базовый Django-проект для сайта частного гида в Швейцарии.

## Быстрый старт

```bash
poetry install
poetry run python manage.py migrate
poetry run python manage.py runserver
```

Или через Docker:

```bash
docker compose up --build
```
